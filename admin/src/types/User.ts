export type UserLocalStorage = {
    userName: string
    email: string,
    role: string,
}

export type User = {
    id: string,
    firstName: string,
    lastName: string,
    address: string,
    dateOfBirth: Date,
    role: string,
    phoneNumber: string,
}